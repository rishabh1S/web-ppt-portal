import { ApplyStylesDirective } from './apply-styles.directive';

describe('ApplyStylesDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyStylesDirective();
    expect(directive).toBeTruthy();
  });
});
